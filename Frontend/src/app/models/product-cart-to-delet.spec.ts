import { ProductCartToDelet } from './product-cart-to-delet';

describe('ProductCartToDelet', () => {
  it('should create an instance', () => {
    expect(new ProductCartToDelet()).toBeTruthy();
  });
});
